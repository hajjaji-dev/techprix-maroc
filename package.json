{
  "name": "techprix-maroc",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "build": "npx prisma generate && npx prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "cors": "^2.8.5",
    "express": "^4.19.0",
    "prisma": "^5.14.0"
  },
  "engines": { "node": ">=20" }
}
